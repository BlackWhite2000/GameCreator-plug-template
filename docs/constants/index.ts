const app = '名称'
const appType = '类型'
const appTypeId = 0
const appName = `${app}${appType}文档`
const appDescription = `${app}${appType}文档`

export {
  app,
  appName,
  appType,
  appTypeId,
  appDescription,
}
